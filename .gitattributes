# Concurrent writers (Actions jobs, Claude Code sessions) all commit under
# data/. See .github/scripts/commit-data.sh for how each class merges.
data/archive/*.jsonl merge=union
data/state.json merge=state
data/anthropic-usage.json merge=ledger
