node_modules/
# local ONNX embedding model (35 MB, fetched by `npm run download-model`)
data/models/
.claude/settings.local.json
data/backfill-progress.json
.claude/worktrees/

# eval — the crosswalk holds the answer key; never commit it
eval/keys/
eval/verdicts/
eval/out/
eval/__pycache__/
